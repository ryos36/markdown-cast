;; 複合語辞書 — mapcar --dic で参照される
;;
;; mecab が分割してしまう語をここに追加して 1 語に畳む。
;; 字幕と TTS の両方に効く。
;;
;; 書き方: (("語1") ("語2") ...)
;;
;; 例:
;;   (("micro:bit")
;;    ("Switch Science")
;;    ("Raspberry Pi"))

()
