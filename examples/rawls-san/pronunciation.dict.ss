;; 読み辞書 — tts2wav --dic で参照される
;;
;; 表記と異なる読みをさせたい語をここに追加する。
;; SSML の読み上げ側だけに適用され、字幕の表記はそのまま残る。
;;
;; 書き方: (("表記" . "読み") ...)  -- ドット対のリスト。ドットを忘れずに。
;; 誤り例: ("表記" "読み")          -- ドットなしはエラーになる。
;;
;; 例:
;;   (("micro:bit" . "マイクロビット")
;;    ("Switch Science" . "スイッチサイエンス"))

()
